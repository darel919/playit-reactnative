import {useSelector} from 'react-redux'
// import {View, Text} from 'react-native'
// const radio = useSelector(state => state.radioPlaying.title)

export default function rNR() {
    // console.log(radio)
    // return useSelector(state => state.radioPlaying.title)
    return 'Now Playing'
}
